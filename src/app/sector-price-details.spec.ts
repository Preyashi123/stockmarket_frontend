import { SectorPriceDetails } from './sector-price-details';

describe('SectorPriceModel', () => {
  it('should create an instance', () => {
    expect(new SectorPriceDetails()).toBeTruthy();
  });
});

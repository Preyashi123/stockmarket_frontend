import { Stockexchangecompany } from './stockexchangecompany';

describe('Stockexchangecompany', () => {
  it('should create an instance', () => {
    expect(new Stockexchangecompany()).toBeTruthy();
  });
});
